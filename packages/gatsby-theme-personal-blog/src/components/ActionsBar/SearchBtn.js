import React from 'react';

import { FiSearch } from 'react-icons/fi';

import IconButton from './IconButton';

export default () => {
  return (
    <IconButton>
      <FiSearch />
    </IconButton>
  );
};
