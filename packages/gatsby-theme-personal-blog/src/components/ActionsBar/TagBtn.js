import React from 'react';

import { FiTag } from 'react-icons/fi';

import IconButton from './IconButton';

export default () => {
  return (
    <IconButton>
      <FiTag />
    </IconButton>
  );
};
