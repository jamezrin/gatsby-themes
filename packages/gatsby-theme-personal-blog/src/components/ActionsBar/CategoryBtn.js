import React from 'react';

import { FiFolder } from 'react-icons/fi';

import IconButton from './IconButton';

export default () => {
  return (
    <IconButton>
      <FiFolder />
    </IconButton>
  );
};
